<div>
    <h3>test header</h3>

    <ul>
        {% for navigationItem in this.getNavigationTree %}
            <li><a href="{{ navigationItem.url }}">{{ navigationItem.name }}</a></li>
        {% endfor %}
    </ul>
</div>