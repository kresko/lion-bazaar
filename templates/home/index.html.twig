{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="home">
        <h1 class="home--title">{{ title }}</h1>
        <p class="home--paragraph">{{ subtitle }}</p>
    </div>
{% endblock %}